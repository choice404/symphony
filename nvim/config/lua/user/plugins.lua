-- Your plugins, lazy.nvim specs, this file is written once and never touched by an update
-- Edit it from the config page with p, new plugins load on the next start, :Lazy installs them
return {
  -- A colorscheme to pick from with T on the config page, for example
  -- { "EdenEast/nightfox.nvim" },
  -- { "rose-pine/neovim", name = "rose-pine" },

  -- Anything else
  -- { "folke/zen-mode.nvim", cmd = "ZenMode" },
}

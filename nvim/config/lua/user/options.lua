-- Your options, loaded after symphony's and LazyVim's, this file is written once and never touched by an update
-- Edit it from the config page with o, every write applies it straight away

-- The system clipboard is the default register
vim.opt.clipboard = "unnamedplus"
-- The file name in the window bar
vim.opt.winbar = "%=%m %f"

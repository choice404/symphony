-- Your colors, this file is written once and never touched by an update
-- Edit it from the config page with t, or press T there to pick a colorscheme and have this file updated, every write applies it
return {
  -- Any colorscheme that is installed, LazyVim ships tokyonight and catppuccin
  colorscheme = "tokyonight-moon",
  -- dark or light
  background = "dark",
  -- Highlight groups set on top of the colorscheme, the same table nvim_set_hl takes
  highlights = {
    -- Comment = { italic = true },
    -- Normal = { bg = "none" },
  },
}
